name: Fetch Hours

on:
  schedule:
    - cron: '05 13 * * *'
    
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v3

    - name: Fetch Hours
      run: |
        ./fetch.sh
